# Docker environment overrides for inter-service communication
# These override the localhost values in individual .env files when running in Docker

# Gateway API overrides
AUTH_SERVICE_API=http://auth-microservice:5000/api/v1
USER_SERVICE_API=http://user-microservice:5000/api/v1
FILE_SERVICE_API=http://file-microservice:3003/api/v1
PROJECT_SERVICE_API=http://project-microservice:5000/api/v1
TASK_SERVICE_API=http://task-microservice:5000/api/v1
NOTIFICATION_SERVICE_API=http://notification-microservice:6432/api/v1
ANALYTICS_SERVICE_API=http://analytics-microservice:5000/api/v1
CORS_ORIGIN=http://localhost:5173

# Auth Microservice overrides
MAIL_SERVICE_HOST=http://mail-microservice
MAIL_SERVICE_PORT=5000
CORS_ORIGIN=http://gateway-api:4000

# Analytics Microservice overrides
# (Add any inter-service variables here if needed)

# File Microservice overrides
CORS_ORIGIN=http://gateway-api:4000

# Mail Microservice overrides
# (Add any inter-service variables here if needed)

# Notification Microservice overrides
# (Add any inter-service variables here if needed)

# Project Microservice overrides
# (Add any inter-service variables here if needed)

# Task Microservice overrides
# (Add any inter-service variables here if needed)

# User Microservice overrides
CORS_ORIGIN=http://gateway-api:4000