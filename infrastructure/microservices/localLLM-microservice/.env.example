PORT=7000
NODE_ENV=development
CORS_ORIGIN=http://localhost:4000

# LM Studio OpenAI-compatible base
LMSTUDIO_BASE_URL=http://localhost:1234/v1
LMSTUDIO_MODEL=gemma-3-1b-it
LMSTUDIO_TIMEOUT_MS=60000

# Spec says prompts must be in English
ENFORCE_ENGLISH=true

# Optional shared secret so ONLY Gateway/allowed services can call this.
# If you won't use it, leave empty and set ENFORCE_INTERNAL_AUTH=false
INTERNAL_API_KEY=change_me
ENFORCE_INTERNAL_AUTH=true
